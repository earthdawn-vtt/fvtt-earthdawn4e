       todos: 
        refactoring: info für delete ability: "no LP tracking adaptations", mit hinzufügen an die warnung.
        refactoring: ability dropdown shall show summary instead of full description.
        potential refactoring: hide and show shall show all effects to drag and drop them.
        Feature: Karma ritual once per day otherwise warning and return. Reset durch overnight rest
        Feature: block karma als boolean | block karma ritual als boolean | block devotion als boolean
        Feature: enable edit mode = True for everything (Edit of items = true, zum nachlesen etc. Items brauchen einen eigenen edit mode)
        Feature: Skill split - based on type (general, artisan & language, knowledge,)
        Feature: group Spells by Circle inside of the tab (config)
        Feature: split funktionen im false mode alphabetisch.
        Feature: add language trigger speak Language | read-/write Language falls die anzahl der Sprachen <= dem Rang ist.
        Feature: Initiative Roll via button on char sheet shall trigger combat encounter roll, if a running encounter is active.
        Refactor: add knacks to none-split mode of talents (ich glaub das ist nicht drin)
        Refactor: move legend tab to notes (not the attribute increase) || rename notes Tab to legend
        Refactor: move the attribute increase to the edit mode of the attributes secion in the general tab itself.

        Herausfinden: was passiert wenn ich einen bereitsabgestimmten Zauber abstimmen will? ist die option dann überhaupt da?
        Herausfinden: rotate item status still broken?


nach der Beta:
1. Bug fixing from Beta
2. Clean up of initital release issues

Initial release!
1. Module umsetzten


       