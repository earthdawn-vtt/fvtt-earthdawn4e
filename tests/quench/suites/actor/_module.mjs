import creation from "./creation.quench.mjs";
// import properties from "./properties.mjs";

/**
 * Actor test suite.
 * @param context
 */
export default ( context ) => {
  creation( context );
  // properties(context);
};
