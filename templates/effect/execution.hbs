  <section class="tab{{#if tab.active}} active{{/if}}" data-group="{{tab.group}}" data-tab="{{tab.id}}">

    {{formGroup systemFields.execution.fields.executeOn value=source.system.execution.executeOn rootId=rootId localize=true}}

    {{formGroup systemFields.execution.fields.script value=source.system.execution.script rootId=rootId localize=true}}

    <button type="button" data-action="execute">
      <i class="fa-regular fa-play" inert></i>
      <!--label></label-->
    </button>

  </section>